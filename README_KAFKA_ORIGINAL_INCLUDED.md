# Included source snapshot

This bundle includes the original project snapshot inside `kafka_original/`.
Some upstream repository entries were symbolic links/submodule-backed paths in the source snapshot.
Where those were unavailable in the snapshot, placeholder `.missing_symlink.txt` notes were preserved instead.
